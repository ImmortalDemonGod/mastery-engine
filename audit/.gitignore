# Forensic pipeline: ignore transient run scratch/logs; keep deliverables
# (0N-*.md), the orchestrator script, resume state (.work/state.json) and
# the schema-validated stage objects (.work/0N-*.json).
.work/agent_*.json
.work/*.log
*.log
