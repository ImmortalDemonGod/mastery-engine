# Bug Symptom: Training Loop — training-loop-missing-zero-grad

## Observed Behavior
A subtle semantic bug has been injected into your `train_loop` implementation. Your code
runs without raising a syntax error, but training no longer converges and the module's
correctness test fails: the loss fails to decrease (it may even grow).

## Your Challenge
A single statement is missing from `cs336_basics/training.py`. PyTorch accumulates
gradients by default — what must happen to each parameter's `.grad` BEFORE every
`loss.backward()`/`optimizer.step()`? Restore the missing call in the right place.

## Verify
Run `mastery submit` to re-run the validator once you believe the bug is fixed.
